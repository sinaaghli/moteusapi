# Set SOURCES variable
file(GLOB MOTEUS_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cc)
file(GLOB MOTEUSAPI_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
list(APPEND SOURCES ${MOTEUS_SOURCES} ${MOTEUSAPI_SOURCES})

# # Set HEADERS variable
file(GLOB HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

include(${CMAKE_SOURCE_DIR}/cmake/LibraryConfig.cmake)
